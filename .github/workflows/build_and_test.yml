name: Build & Test

on:
  push:
    branches:
      - master
      - dev
  pull_request:
    branches:
      - master
      - dev
      - workflow/ci

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-go@v2
        with:
          go-version: 1.18

      - name: Run tests (generic parser)
        run: go test -race -tags=generic_parser ./...

      - name: Run tests (SIMD parser)
        run: go test -race -tags=!generic_parser ./...
